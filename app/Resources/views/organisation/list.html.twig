{% extends 'base.html.twig' %}

{% block body %}
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Organisation</th>
                <th>Domain</th>
            </tr>
        </thead>
        <tbody>
            {% for organisation in organisations %}
                <tr>
                    <td>{{ organisation.organisation_name }}</td>
                    <td>{{ organisation.organisation_domain }}</td>

                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}